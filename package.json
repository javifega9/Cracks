{
  "name": "cracks",
  "version": "2.0.0",
  "private": true,
  "description": "Cracks: buscador de ofertas con arquitectura hibrida AI + scraping.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js",
    "install:browsers": "playwright install chromium"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "express": "^4.21.2",
    "openai": "^5.12.2",
    "playwright": "^1.54.2"
  }
}
